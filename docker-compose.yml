version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    depends_on:
      - db
    links:
      - db
    env_file:
      - .env
    volumes:
      - ./static:/app/static
    